package com.iseen.archives.common.pack;

import com.iseen.archives.core.pojo.IArchives;
import com.centling.common.util.UtilWork;

public class IarchivesPack{

    public static String packIArchivesQuery(IArchives iArchives){
        StringBuffer result = new StringBuffer();
        //result.append(" order by model.recordDate desc");
        return result.toString();
    }

}